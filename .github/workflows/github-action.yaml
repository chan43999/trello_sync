name: Build Image using Containerfile
on: [push]

jobs:
  build:
    name: Build image
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Buildah Action
        uses: redhat-actions/buildah-build@v2
        with:
          image: chan43999/trello_sync
          tags: ${{ github.sha }}
          containerfiles: |
            ./Dockerfile